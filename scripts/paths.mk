####################
# PATH DEFINITIONS #
####################

PREFIX ?= .

PATH_SCRIPTS := ${PREFIX}/scripts
PATH_ARCH    := ${PREFIX}/arch
PATH_DRIVERS := ${PREFIX}/drivers
PATH_PLAT    := ${PREFIX}/plat
PATH_TARGETS := ${PREFIX}/targets
PATH_TOOLS   := ${PREFIX}/tools
PATH_OUT     := ${PREFIX}/out

PATHS += ${PATH_SCRIPTS}
PATHS += ${PATH_ARCH}
PATHS += ${PATH_DRIVERS}
PATHS += ${PATH_PLAT}
PATHS += ${PATH_TARGETS}
PATHS += ${PATH_TOOLS}
