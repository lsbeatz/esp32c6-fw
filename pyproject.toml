[project]
name = "esp32c6-fw"
version = "0.1.0"
description = "Python tools for esp32c6"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyserial>=3.5",
    "toml>=0.10.2",
]

[tool.black]
line-length = 120
